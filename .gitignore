# Downloaded source data — never committed.
# Consumed once by backend/cmd/seed to populate the catalog + EAN tables.
# Anchored to the repo root so it does not swallow the app's lib/data package.
/data/

# Env / secrets
.env
.env.*
!.env.example

# Go
/backend/bin/
*.test
*.out
coverage.*

# Flutter / Dart
/app/.dart_tool/
/app/build/
/app/.flutter-plugins
/app/.flutter-plugins-dependencies
**/*.g.dart.bak
.packages

# fvm
.fvm/

# AWS / IaC
/infra/.terraform/
*.tfstate
*.tfstate.*
.aws-sam/
samconfig.toml

# Editor / OS
.DS_Store
*.swp
